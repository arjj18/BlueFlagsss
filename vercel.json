{
  "buildCommand": "cd artifacts/pit-lane && npm install && npm run build",
  "outputDirectory": "artifacts/pit-lane/dist",
  "installCommand": "npm install",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
