{
  "buildCommand": "pnpm --filter pit-lane build",
  "outputDirectory": "artifacts/pit-lane/dist",
  "installCommand": "npm install -g pnpm && pnpm install",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
